Security changes applied:

- Attempted to integrate Spatie Laravel Permission but composer constraints prevented installation in this environment. Reverted bootstrap middleware to existing custom RoleMiddleware to keep app functional.
- Added input sanitization and dedicated request classes for registration and seller requests (`RegisterRequest`, `SellerRequest`).
- Updated `User` to include `HasRoles` trait and guard_name; role synchronization added but guarded by `method_exists` so it won't error if Spatie isn't installed.
- Added HTTPS enforcement in `AppServiceProvider` (`URL::forceScheme('https')` in production).
- Added Spatie config and migration files (left in repo) — if you want to fully enable Spatie, run `composer require spatie/laravel-permission` with a compatible PHP/Illuminate combo, then run `php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider" --tag="config"` and `php artisan migrate`.
- Ensured audit logs include `ip_address` where controllers already create them.

Next recommended steps:
- Install a compatible Spatie package version matching your PHP and Laravel versions, then run migrations.
- Review `User` model to remove legacy `role` column or keep it in sync with Spatie roles.
- Run `composer install` and `php artisan migrate` on your environment to apply DB changes.
